name: MLflow CI/CD with kind

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  mlflow-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Install kind
        run: |
          curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.20.0/kind-linux-amd64
          chmod +x ./kind
          sudo mv ./kind /usr/local/bin/kind
          kind version

      - name: Create kind cluster
        run: kind create cluster --name mlflow-cluster

      - name: Build MLflow Docker image
        run: |
          docker build -t mlflow-app:latest .
          kind load docker-image mlflow-app:latest --name mlflow-cluster

      - name: Create mlflow namespace
        run: |
          kubectl create namespace mlflow || echo "Namespace already exists"

      - name: Apply Kubernetes resources
        run: |
          kubectl apply -f k8s/mlflow-deployment.yaml -n mlflow
          kubectl apply -f k8s/mlflow-service.yaml -n mlflow
          kubectl apply -f k8s/mlflow-job.yaml -n mlflow

      - name: Wait for MLflow Job pod
        run: |
          kubectl wait --for=condition=ready pod -l job-name=mlflow-job -n mlflow --timeout=180s

      - name: Get logs from MLflow Job
        run: |
          POD=$(kubectl get pods -n mlflow -l job-name=mlflow-job -o jsonpath='{.items[0].metadata.name}')
          kubectl logs $POD -n mlflow -f

      - name: Verify all MLflow pods
        run: kubectl get pods -n mlflow
